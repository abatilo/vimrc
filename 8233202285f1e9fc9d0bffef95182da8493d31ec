## Conversation
This change was already present as an unstaged modification when the user
started the conversation. Not explicitly requested — committed alongside
the commit-notes rule work as a separate atomic commit.

## Actions
1. Noticed .gitconfig_global modification in git status.
2. Diff showed git-lfs filter config (clean, smudge, process, required).
3. Committed separately from the rules change.

## Errors & Mistakes
None.

## Dead Ends
None.

## Hints for Future Agents
This is likely auto-generated by git-lfs installation. The config lives
in .gitconfig_global which is symlinked into the home directory.

## Codebase Discoveries
None.

## Open Questions
None.
